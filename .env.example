PORT=3001

# Extra allowed origins, comma-separated. The Vercel site and localhost
# are already allowed in code.
FRONTEND_ORIGIN=

# Folder that should receive guest photos.
# Share this folder with the service account email as Editor.
GOOGLE_DRIVE_FOLDER_ID=

# On Railway, paste the full service account JSON as one line.
# Do not use GOOGLE_APPLICATION_CREDENTIALS there — that path only works locally.
GOOGLE_SERVICE_ACCOUNT_JSON=

# Local only: path to the JSON key file
# GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
